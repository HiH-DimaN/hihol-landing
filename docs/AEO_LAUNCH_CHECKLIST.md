# Чек-лист запуска AEO-дельты

Относится к коммитам `2f53e27` (answer-first блоки, таблицы, FAQ 12, один JSON-LD @graph)
и `f864f2d` (синхронизация контрактов тестов). Ветка смерджена в `main`.

Порядок важен: пока сайт не задеплоен, пункты 2-4 бессмысленны - проверять и
отдавать на переобход нечего.

## 1. Публикация (владелец)

- [ ] `git push origin main` - при блокировке ITD pre-push гейтом использовать
      `git push --no-verify origin main` (репозиторий не зарегистрирован в реестре
      гейта; обход санкционирован владельцем 2026-08-20).
- [ ] Coolify -> приложение `vbi7465l8gbsidb5l0npgek1` -> Deploy. Автодеплоя нет.
- [ ] Дождаться завершения сборки и проверить, что открывается https://hihol.ru/

## 2. Проверки по живому URL (5 минут)

- [ ] Разметка: https://validator.schema.org/ -> вставить URL https://hihol.ru/ ->
      ошибок 0. Ожидаемые сущности: Service, WebPage, FAQPage (страница) плюс
      Person, WebSite, ProfessionalService (общесайтовые).
- [ ] Расширенные результаты Google: https://search.google.com/test/rich-results
      -> URL -> блок FAQ распознан, 12 вопросов.
- [ ] Контент без JS:

```bash
curl -s https://hihol.ru/ | grep -c "<table"
```

      Ожидание: `3` (штрафы, сканер vs аудит, тарифы).

```bash
curl -s https://hihol.ru/ | grep -c "Предотмеченная галочка"
```

      Ожидание: `1` - последний, 12-й ответ FAQ присутствует в HTML.
- [ ] Lighthouse (Chrome DevTools -> Lighthouse -> Mobile): Performance и
      Accessibility >= 90.
- [ ] robots.txt отдается и не запрещает ботов:

```bash
curl -s https://hihol.ru/robots.txt
```

## 3. Индексация (в тот же день)

Без этого шага AEO не работает: движки цитируют то, что есть в индексах.

- [ ] **Bing Webmaster Tools** (https://www.bing.com/webmasters) - добавить сайт,
      подтвердить владение, отправить sitemap. Критично: ChatGPT ищет через индекс
      Bing, а не Google.
- [ ] **IndexNow** в том же кабинете - отправить https://hihol.ru/ на переобход.
- [ ] **Яндекс.Вебмастер** - переобход главной (влияет на Алису и Нейро,
      приоритетный движок для РФ).
- [ ] **Google Search Console** - "Проверка URL" -> "Запросить индексирование".

## 4. Аналитика (в тот же день)

- [ ] Яндекс.Метрика (счетчик 108695727) -> Отчеты -> Источники -> Сайты ->
      сегмент по регулярному выражению, сохранить как отчет "Трафик из нейросетей":

```
chatgpt\.com|openai\.com|claude\.ai|perplexity|copilot\.microsoft\.com|gemini\.google\.com|deepseek\.com|ya\.ru|giga\.chat|sber\.ru
```

- [ ] Базовый замер видимости в ИИ - см. отдельный файл
      `zamer_vidimosti_ai_2026-08.md` в рабочем репозитории направления.
      Делать ДО того, как движки переобойдут сайт: это точка отсчета.

## 5. Квартальное обслуживание

Дата актуальности живет в одной константе `app/lib/site.ts`:
`DATE_MODIFIED_SHORT`. Из нее выводятся строки "актуально на", `dateModified`
в JSON-LD, `lastmod` в sitemap и дата в `public/llms.txt`.

Раз в квартал (следующий срок: ноябрь 2026):

- [ ] Сверить суммы штрафов по действующей редакции ст. 13.11 КоАП РФ на
      consultant.ru и цены по действующему прайсу.
- [ ] Обновить `DATE_MODIFIED_SHORT` и `Обновлено:` в `public/llms.txt`
      (тест `metadata, dates, llms and policy tell one current story` следит,
      чтобы даты не разъехались).
- [ ] Повторить замер видимости и сравнить с предыдущим.

## Чего намеренно НЕ делаем

- Не развиваем `llms.txt` - эффект не подтвержден (SE Ranking, 129 тыс. доменов).
- Не добавляем HowTo и Speakable - Google обесценил HowTo, Speakable не про РФ.
- Не используем скрытые промпты в HTML - это клоакинг и риск санкций.
- Не добавляем разметку Review и AggregateRating, пока нет реальных отзывов.
